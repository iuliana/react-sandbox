
// this is a function component
function Alert1() {
    return (
        <div>
            <div>
                <span role="img" aria-label="Warning">⚠</span>
                <span>[Alert1]Oh no!</span>
            </div>
            <div>Something isn't quite right ...</div>
        </div>
    );
}

export default Alert1;

// default imports do not need curly braces
